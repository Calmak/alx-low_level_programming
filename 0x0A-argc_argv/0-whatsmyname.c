#include <stdio.h>

/**
 * main - print its name
 * @argc: argc parameter
 * @argv: an array of a command listed
 * Return: Always 0
 */

int main (int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
