#include <stdio.h>
/**
 * main - Entry point
 * Description: Print alphbet in lowercase excluding e and q
 * Return: Aways 0
 */
int main(void)
{
	int n = 97;

	while (n <= 122)
	{
		if (n == 101 || n == 113)
		{
			n++;
			continue;
		}
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
